[project]
name = "skill-orchestrator"
version = "0.1.0"
description = "Self-evolving skill orchestration service"
requires-python = ">=3.9"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
